---
phase: 13-named-styles-focus-mode-gutter-indicators
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - tests/visual/test_visual_regression.py
  - tests/test_highlights_integration.py
autonomous: true
requirements:
  - HLTEST-04
  - HLTEST-06
  - HLTEST-08

must_haves:
  truths:
    - "Visual regression baselines exist for each of the 4 named styles (add, remove, focus, highlight)"
    - "Focus mode dimming has a visual regression snapshot AND a unit test verifying opacity value"
    - "Gutter indicators appear in visual regression snapshots for named styles"
    - "CLI integration tests verify --highlight flag parsing and style rendering"
  artifacts:
    - path: "tests/visual/test_visual_regression.py"
      provides: "Visual regression tests for add, remove, focus, highlight styles plus gutter indicators"
      contains: "test_highlight_style_add"
    - path: "tests/test_highlights_integration.py"
      provides: "CLI integration tests for --highlight flag and focus mode"
      contains: "test_focus_mode"
  key_links:
    - from: "tests/visual/test_visual_regression.py"
      to: "src/codepicture/render/renderer.py"
      via: "renders code with highlight styles and compares against baselines"
      pattern: "highlight.*add|highlight.*remove|highlight.*focus"
    - from: "tests/test_highlights_integration.py"
      to: "src/codepicture/cli/app.py"
      via: "invokes CLI with --highlight flags"
      pattern: "--highlight"
---

<objective>
Add visual regression tests for each named highlight style, focus mode dimming, and gutter indicators, plus CLI integration tests for the --highlight flag.

Purpose: Verify all visual outputs match expected baselines and CLI integration works end-to-end, completing the testing requirements for Phase 13.
Output: Extended visual regression test suite with 4+ style-specific baselines and integration tests for focus mode and gutter indicators.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-named-styles-focus-mode-gutter-indicators/13-CONTEXT.md
@.planning/phases/13-named-styles-focus-mode-gutter-indicators/13-RESEARCH.md
@.planning/phases/13-named-styles-focus-mode-gutter-indicators/13-01-SUMMARY.md
@.planning/phases/13-named-styles-focus-mode-gutter-indicators/13-02-SUMMARY.md

@tests/visual/test_visual_regression.py
@tests/visual/conftest.py
@tests/test_highlights_integration.py
@tests/test_highlights.py

<interfaces>
<!-- From Plans 01+02: key interfaces for testing -->
From src/codepicture/render/highlights.py:
```python
class HighlightStyle(str, Enum):
    HIGHLIGHT = "highlight"
    ADD = "add"
    REMOVE = "remove"
    FOCUS = "focus"

FOCUS_DIM_OPACITY = 0.35
def parse_highlight_specs(specs, total_lines, line_number_offset=1) -> dict[int, HighlightStyle]: ...
def resolve_style_color(style, style_overrides=None) -> Color: ...
```

From src/codepicture/config/schema.py:
```python
class RenderConfig(BaseModel):
    highlights: list[str] | None = None
    highlight_styles: dict[str, HighlightStyleConfig] | None = None
```

From src/codepicture/render/renderer.py:
```python
def _dim_color(color: Color, factor: float) -> Color: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add visual regression tests for each named style and gutter indicators (HLTEST-04, HLTEST-08)</name>
  <files>tests/visual/test_visual_regression.py</files>
  <read_first>
    - tests/visual/test_visual_regression.py
    - tests/visual/conftest.py
    - src/codepicture/render/renderer.py
    - src/codepicture/config/schema.py
  </read_first>
  <action>
Extend tests/visual/test_visual_regression.py with visual regression tests for named styles. Follow the existing pattern in the file for how visual regression tests are structured (using the snapshot/pixelmatch infrastructure from conftest.py).

Add a new test class or group of tests (follow existing file's pattern):

1. **test_highlight_style_add** -- render a small Python snippet with `highlights=["2-3:add"]`, verify against baseline. Config: `RenderConfig(highlights=["2-3:add"], show_line_numbers=True)`. This tests green background + "+" gutter indicator.

2. **test_highlight_style_remove** -- render with `highlights=["2:remove"]`, verify against baseline. Tests red background + "-" gutter indicator.

3. **test_highlight_style_focus** -- render with `highlights=["2:focus"]`, verify against baseline. Tests blue background + colored bar gutter indicator + dimmed unfocused lines.

4. **test_highlight_style_highlight** -- render with `highlights=["2:highlight"]`, verify against baseline. Tests yellow background + colored bar gutter indicator. (This is the default style.)

5. **test_highlight_style_mixed** -- render with `highlights=["1:add", "3:remove", "5:focus"]`, verify against baseline. Tests multiple styles in one image.

6. **test_gutter_indicators_visible** -- render with `highlights=["1:add", "2:remove", "3:focus", "4:highlight"]`, verify gutter column appears with +, -, bar, bar respectively.

Each test should use a consistent small code fixture (e.g., a 5-line Python snippet). Use PNG format for visual comparison. Run with `--snapshot-update` on first run to generate baselines.

Per D-14: one snapshot baseline per style (4 images minimum).

After creating tests, run with `--snapshot-update` to generate initial baselines:
```bash
pytest tests/visual/test_visual_regression.py -x -q -k "highlight_style" --snapshot-update
```
Then run without `--snapshot-update` to verify they match:
```bash
pytest tests/visual/test_visual_regression.py -x -q -k "highlight_style"
```
  </action>
  <verify>
    <automated>cd /Users/bartel/Documents/newclone/codepicture && python -m pytest tests/visual/test_visual_regression.py -x -q -k "highlight_style or gutter" --snapshot-update && python -m pytest tests/visual/test_visual_regression.py -x -q -k "highlight_style or gutter"</automated>
  </verify>
  <acceptance_criteria>
    - test_visual_regression.py contains `test_highlight_style_add`
    - test_visual_regression.py contains `test_highlight_style_remove`
    - test_visual_regression.py contains `test_highlight_style_focus`
    - test_visual_regression.py contains `test_highlight_style_highlight`
    - test_visual_regression.py contains `test_highlight_style_mixed`
    - test_visual_regression.py contains `test_gutter_indicators_visible`
    - Visual baseline images generated in tests/visual/references/ directory
    - `pytest tests/visual/test_visual_regression.py -x -q -k "highlight_style"` exits 0
    - At least 4 new reference images exist for the 4 individual style tests
  </acceptance_criteria>
  <done>Visual regression tests exist for all 4 named styles individually, mixed styles, and gutter indicators. Baselines generated and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for focus mode dimming and CLI --highlight flag (HLTEST-06)</name>
  <files>tests/test_highlights_integration.py</files>
  <read_first>
    - tests/test_highlights_integration.py
    - tests/test_highlights.py
    - src/codepicture/cli/app.py
    - src/codepicture/render/renderer.py
  </read_first>
  <action>
Extend tests/test_highlights_integration.py with tests for the new --highlight flag and focus mode. Keep existing tests. Add:

1. **TestHighlightFlagCLI** -- CLI integration tests using Typer's CliRunner (follow existing pattern in the file):
   - test_highlight_flag_single_style: `--highlight '3:add'` produces output without error
   - test_highlight_flag_multiple: `--highlight '1:add' --highlight '3:remove'` succeeds
   - test_highlight_flag_no_style_defaults: `--highlight '3'` succeeds (defaults to highlight style)
   - test_highlight_flag_invalid_style: `--highlight '3:bogus'` exits with error
   - test_old_highlight_lines_flag_removed: `--highlight-lines '3'` exits with error (flag no longer exists)

2. **TestFocusModeDimming** -- integration tests for focus mode (per D-15):
   - test_focus_mode_produces_output: render with `highlights=["2:focus"]` produces non-empty PNG output
   - test_focus_dim_opacity_value: verify FOCUS_DIM_OPACITY == 0.35 (imported from highlights module)
   - test_dim_color_function: import `_dim_color` from renderer, verify `_dim_color(Color(255, 255, 255, 200), 0.35)` produces Color with alpha == 70
   - test_focus_with_other_styles: render with `highlights=["2:focus", "4:add"]` produces output -- both focus and add lines should be fully visible, others dimmed
   - test_no_focus_no_dimming: render with `highlights=["2:add"]` (no focus style) -- _dim_color should not be applied (verify by checking that style_map values contain no FOCUS)

3. **TestGutterIndicatorIntegration** (HLTEST-08):
   - test_gutter_with_add_style: render with highlights=["1:add"] and show_line_numbers=True, verify output is non-empty
   - test_gutter_hidden_without_line_numbers: render with highlights=["1:add"] and show_line_numbers=False, verify output produces (no crash)
   - test_gutter_with_all_styles: render with highlights=["1:add", "2:remove", "3:focus", "4:highlight"], verify output is non-empty

For render-based tests (not CLI), use the same pattern as existing integration tests: create RenderConfig, call the rendering pipeline, verify non-empty output bytes.
  </action>
  <verify>
    <automated>cd /Users/bartel/Documents/newclone/codepicture && python -m pytest tests/test_highlights_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_highlights_integration.py contains `class TestHighlightFlagCLI` with 5 test methods
    - test_highlights_integration.py contains `class TestFocusModeDimming` with 5 test methods
    - test_highlights_integration.py contains `class TestGutterIndicatorIntegration` with 3 test methods
    - test_highlights_integration.py contains `test_old_highlight_lines_flag_removed`
    - test_highlights_integration.py contains `test_focus_dim_opacity_value` that asserts FOCUS_DIM_OPACITY == 0.35
    - test_highlights_integration.py contains `test_dim_color_function` that verifies alpha calculation
    - `pytest tests/test_highlights_integration.py -x -q` exits 0
  </acceptance_criteria>
  <done>CLI integration tests verify --highlight flag works, old --highlight-lines is removed, focus mode produces output with correct dimming. Gutter indicator integration tests verify rendering with all style combinations. All tests pass.</done>
</task>

</tasks>

<verification>
1. `pytest tests/visual/test_visual_regression.py -x -q -k "highlight_style or gutter"` passes
2. `pytest tests/test_highlights_integration.py -x -q` passes
3. `pytest tests/ -x -q --timeout=60` full suite passes
</verification>

<success_criteria>
- 4+ visual regression baselines exist for named styles (add, remove, focus, highlight)
- Mixed style visual test verifies multiple styles in one image
- Gutter indicator visual test verifies +/-/bar markers
- Focus mode has both visual regression snapshot AND unit test for opacity value
- CLI integration tests verify --highlight flag and rejection of --highlight-lines
- Full test suite passes: `pytest tests/ -x -q --timeout=60`
</success_criteria>

<output>
After completion, create `.planning/phases/13-named-styles-focus-mode-gutter-indicators/13-03-SUMMARY.md`
</output>
