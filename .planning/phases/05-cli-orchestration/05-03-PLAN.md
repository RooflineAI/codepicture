---
phase: 05-cli-orchestration
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/codepicture/__main__.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "python -m codepicture runs the CLI"
    - "codepicture command is available after pip install"
    - "Help text displays when run with no args"
  artifacts:
    - path: "src/codepicture/__main__.py"
      provides: "Module entry point"
      contains: "from .cli import app"
      min_lines: 5
    - path: "pyproject.toml"
      provides: "Console script entry point"
      contains: "codepicture"
  key_links:
    - from: "__main__.py"
      to: "cli/app.py"
      via: "app import and call"
      pattern: "from.*cli.*import.*app"
---

<objective>
Create entry points for running codepicture as a command.

Purpose: Enable users to run `python -m codepicture` and `codepicture` command after installation.

Output: __main__.py entry point and pyproject.toml console script configuration
</objective>

<execution_context>
@/Users/bartel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bartel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-orchestration/05-CONTEXT.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create __main__.py entry point</name>
  <files>src/codepicture/__main__.py</files>
  <action>
Create `src/codepicture/__main__.py`:

```python
"""Entry point for running codepicture as a module.

Usage: python -m codepicture [ARGS]
"""

from .cli import app

if __name__ == "__main__":
    app()
```

This enables `python -m codepicture` invocation.
  </action>
  <verify>
```bash
cd /Users/bartel/Documents/newclone/codepicture && uv run python -m codepicture --help
```
Should show help text for the CLI.
  </verify>
  <done>
- __main__.py exists
- `python -m codepicture --help` shows CLI help
  </done>
</task>

<task type="auto">
  <name>Task 2: Add console script entry point</name>
  <files>pyproject.toml</files>
  <action>
Add console script entry point to pyproject.toml:

```toml
[project.scripts]
codepicture = "codepicture.cli:app"
```

This enables the `codepicture` command to be available after `pip install`.

Then reinstall the package in development mode to register the entry point:
```bash
uv sync
```
  </action>
  <verify>
```bash
cd /Users/bartel/Documents/newclone/codepicture && uv sync && uv run codepicture --help
```
Should show help text for the CLI.
  </verify>
  <done>
- pyproject.toml has [project.scripts] section with codepicture entry
- `codepicture --help` works after uv sync
  </done>
</task>

</tasks>

<verification>
All checks must pass:
```bash
cd /Users/bartel/Documents/newclone/codepicture
uv run python -m codepicture --help         # Module invocation
uv run codepicture --help                   # Console script
uv run codepicture --version                # Version flag
uv run codepicture --list-themes            # List themes
```
</verification>

<success_criteria>
1. `python -m codepicture --help` shows help text
2. `codepicture --help` shows help text (after uv sync)
3. `codepicture --version` prints version
4. `codepicture --list-themes` lists available themes
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-orchestration/05-03-SUMMARY.md`
</output>
