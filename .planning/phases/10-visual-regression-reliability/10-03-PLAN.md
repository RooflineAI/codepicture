---
phase: 10-visual-regression-reliability
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tests/visual/test_reliability_matrix.py
autonomous: true

must_haves:
  truths:
    - "All 5 languages x 3 formats produce valid, non-empty output that completes within timeout"
    - "PNG output starts with PNG magic bytes, SVG output is valid XML, PDF output starts with %PDF"
    - "Feature toggles (shadow, chrome, line numbers) all produce valid output without errors"
    - "Output dimensions are positive and reasonable (width > 0, height > 0)"
  artifacts:
    - path: "tests/visual/test_reliability_matrix.py"
      provides: "Parametrized reliability tests for REL-01 and REL-02"
  key_links:
    - from: "tests/visual/test_reliability_matrix.py"
      to: "tests/visual/conftest.py"
      via: "render_fixture import for pipeline execution"
    - from: "tests/visual/test_reliability_matrix.py"
      to: "src/codepicture/cli/orchestrator.py"
      via: "exercises the same pipeline as generate_image"
---

<objective>
Create parametrized reliability tests covering all language/format/config combinations to ensure every rendering path produces valid output.

Purpose: Covers REL-01 (5 languages x 3 formats) and REL-02 (feature toggle combinations) — systematic verification that the rendering pipeline works for all supported combinations.
Output: Parametrized test suite asserting valid output format, non-empty data, and successful completion for all combinations.
</objective>

<execution_context>
@/Users/bartel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bartel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-regression-reliability/10-01-SUMMARY.md
@tests/visual/conftest.py
@src/codepicture/core/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create language x format reliability matrix tests (REL-01)</name>
  <files>
    tests/visual/test_reliability_matrix.py
  </files>
  <action>
    Create `tests/visual/test_reliability_matrix.py` with:

    1. **Test: `test_render_language_format[{language}-{format}]`**
       Parametrize over 5 languages x 3 formats (15 combinations).

       For each combo:
       a. Build RenderConfig with `output_format=format`
       b. Call `render_fixture(fixture_path, config, language=language)` to get raw bytes
       c. Assert data is not empty (`len(data) > 0`)
       d. Validate format-specific:
          - PNG: starts with `b'\x89PNG'` magic bytes, can be opened by `Image.open(BytesIO(data))`
          - SVG: data decodes as UTF-8 and contains `<svg` tag
          - PDF: starts with `b'%PDF'`
       e. For PNG: open with Pillow and assert dimensions > 0
       f. Mark with `@pytest.mark.timeout(30)`

    2. **Test: `test_render_completes_within_timeout[{language}-{format}]`**
       Same parametrization. Measure wall-clock time and assert < 10s per render.
       This is a sanity check that no language/format combo triggers excessive processing.
  </action>
  <verify>`uv run pytest tests/visual/test_reliability_matrix.py -v -k "test_render_language_format"` — all 15 pass</verify>
  <done>All 15 language x format combinations produce valid, non-empty output with correct format headers. No combination exceeds the timeout.</done>
</task>

<task type="auto">
  <name>Task 2: Create feature toggle reliability tests (REL-02)</name>
  <files>
    tests/visual/test_reliability_matrix.py
  </files>
  <action>
    Add to `test_reliability_matrix.py`:

    1. **Config toggle combinations** (test on Python fixture with PNG format):
       ```python
       TOGGLE_COMBOS = [
           ("all-defaults", {}),
           ("shadow-off", {"shadow": False}),
           ("chrome-off", {"window_controls": False}),
           ("lines-off", {"show_line_numbers": False}),
           ("minimal", {"shadow": False, "window_controls": False, "show_line_numbers": False}),
           ("shadow-on_chrome-off", {"shadow": True, "window_controls": False}),
           ("shadow-off_lines-on", {"shadow": False, "show_line_numbers": True}),
       ]
       ```

    2. **Test: `test_render_toggle_combo[{combo_name}]`**
       Parametrize over TOGGLE_COMBOS (7 combinations).

       For each combo:
       a. Build RenderConfig with PNG format + combo overrides
       b. Render Python fixture
       c. Assert data is not empty, starts with PNG magic bytes
       d. Open with Pillow, assert width > 0 and height > 0
       e. Assert no exceptions raised during rendering
       f. Mark with `@pytest.mark.timeout(30)`

    3. **Test: `test_toggle_dimensions_change`**
       Non-parametrized test verifying that toggles actually change output:
       - Render with shadow=True vs shadow=False: PNG dimensions should differ (shadow adds margin)
       - Render with chrome=True vs chrome=False: height should differ (title bar adds height)
       - Render with line_numbers=True vs line_numbers=False: width should differ (gutter adds width)
  </action>
  <verify>`uv run pytest tests/visual/test_reliability_matrix.py -v` — all tests pass</verify>
  <done>7 toggle combinations all produce valid PNG output. Dimension assertions confirm toggles actually affect rendering output. All reliability matrix tests pass.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/visual/test_reliability_matrix.py -v` — all tests pass
2. `uv run pytest tests/visual/ -v` — full visual test suite passes (regression + reliability)
3. `uv run pytest tests/ -x -q` — entire test suite passes (no regressions)
</verification>

<success_criteria>
- 15 language x format tests pass with valid output format verification
- 7 feature toggle tests pass with valid PNG output
- Dimension change test confirms toggles affect output
- No rendering combination hangs or crashes
- All existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-regression-reliability/10-03-SUMMARY.md`
</output>
