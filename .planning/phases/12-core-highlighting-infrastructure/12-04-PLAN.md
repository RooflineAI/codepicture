---
phase: 12-core-highlighting-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - tests/test_cli.py
  - tests/visual/test_visual_regression.py
  - tests/test_highlights_integration.py
autonomous: true

must_haves:
  truths:
    - "CLI --highlight-lines flag parses correctly and produces highlighted output"
    - "CLI --highlight-color flag customizes highlight color"
    - "Out-of-bounds --highlight-lines causes non-zero exit with error message"
    - "Visual regression baselines exist for highlighted output in PNG, SVG, PDF"
    - "Word-wrapped highlighted lines highlight all display lines in visual output"
    - "TOML config highlight_lines and highlight_color load and render correctly"
  artifacts:
    - path: "tests/test_cli.py"
      provides: "CLI integration tests for highlight flags"
      contains: "highlight"
    - path: "tests/visual/test_visual_regression.py"
      provides: "Visual regression tests for highlight variants"
      contains: "highlight"
    - path: "tests/test_highlights_integration.py"
      provides: "Integration tests for highlight rendering pipeline"
      contains: "highlight"
  key_links:
    - from: "tests/test_cli.py"
      to: "src/codepicture/cli/app.py"
      via: "CliRunner invokes main() with --highlight-lines flag"
      pattern: "--highlight-lines"
    - from: "tests/visual/test_visual_regression.py"
      to: "src/codepicture/render/renderer.py"
      via: "render_fixture produces highlighted images for snapshot comparison"
      pattern: "highlight"
    - from: "tests/test_highlights_integration.py"
      to: "src/codepicture/render/renderer.py"
      via: "full pipeline test with highlight config"
      pattern: "highlight_lines"
---

<objective>
Add comprehensive tests: CLI integration tests for highlight flags, visual regression baselines for highlighted output across all 3 formats, and integration tests for word-wrap + highlighting.

Purpose: Tests verify the full pipeline from CLI flag to rendered output. Visual regression baselines ensure highlights look correct and catch future regressions. Integration tests cover the word-wrap + highlight interaction which is a key correctness concern.

Output: CLI tests for highlight flag parsing and error handling, visual regression test variants for highlights (PNG/SVG/PDF), integration tests for word-wrap + highlight, and reference baseline images.
</objective>

<execution_context>
@/Users/bartel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bartel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-core-highlighting-infrastructure/12-RESEARCH.md
@.planning/phases/12-core-highlighting-infrastructure/12-CONTEXT.md
@.planning/phases/12-core-highlighting-infrastructure/12-03-SUMMARY.md
@tests/test_cli.py
@tests/visual/test_visual_regression.py
@tests/visual/conftest.py
@src/codepicture/config/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI integration tests for highlight flags</name>
  <files>tests/test_cli.py</files>
  <action>
    Read the existing test_cli.py to understand the test patterns (CliRunner, fixture files, tmp_path usage).

    Add new test functions at the end of the file. Follow the existing test style exactly.

    Tests to add:

    1. `test_highlight_lines_single` -- Run CLI with `--highlight-lines 2` on a fixture file, assert exit code 0, assert output file exists and is non-empty.

    2. `test_highlight_lines_multiple` -- Run CLI with `--highlight-lines 1 --highlight-lines 3-5` on a fixture file, assert exit code 0, assert output file created.

    3. `test_highlight_lines_with_color` -- Run CLI with `--highlight-lines 2 --highlight-color '#FF000040'`, assert exit code 0.

    4. `test_highlight_lines_6char_color` -- Run CLI with `--highlight-lines 2 --highlight-color '#FF0000'` (6-char hex), assert exit code 0.

    5. `test_highlight_lines_out_of_bounds` -- Run CLI with `--highlight-lines 999` on a small fixture file, assert exit code 1 (non-zero), assert "out of range" in stderr output.

    6. `test_highlight_lines_invalid_syntax` -- Run CLI with `--highlight-lines abc`, assert exit code is non-zero.

    7. `test_highlight_color_invalid` -- Run CLI with `--highlight-color 'red'`, assert exit code is non-zero.

    Use the same fixture files and patterns as existing CLI tests. Use `tmp_path` for output files. Use the Typer CliRunner from the existing test setup.

    For TOML config test:
    8. `test_highlight_lines_from_toml` -- Write a temporary TOML file with `highlight_lines = ["2"]`, run CLI with `--config {toml_path}`, assert exit code 0.
  </action>
  <verify>
    Run: `python -m pytest tests/test_cli.py -v -k highlight` -- all new highlight tests pass.
    Run: `python -m pytest tests/test_cli.py -v` -- all existing tests still pass.
  </verify>
  <done>
    CLI tests verify: single line, multiple lines, custom color, 6-char color, out-of-bounds error, invalid syntax error, invalid color error, and TOML config loading. All pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add visual regression tests and integration tests for highlights</name>
  <files>tests/visual/test_visual_regression.py, tests/test_highlights_integration.py</files>
  <action>
    **Visual regression tests** -- Add a new HIGHLIGHT_VARIANTS list and parametrized test to `tests/visual/test_visual_regression.py`, following the exact pattern of the existing CONFIG_VARIANTS test:

    ```python
    HIGHLIGHT_VARIANTS = [
        ("highlight-single", {"highlight_lines": ["3"]}),
        ("highlight-range", {"highlight_lines": ["2-4"]}),
        ("highlight-mixed", {"highlight_lines": ["1", "3-5", "7"]}),
        ("highlight-color-red", {"highlight_lines": ["3"], "highlight_color": "#FF000040"}),
        ("highlight-wrap", {"highlight_lines": ["1"], "window_width": 400}),
    ]
    ```

    Add a parametrized test `test_visual_highlight_variant` that:
    - Uses the Python visual fixture (same as config variants)
    - Renders in PNG format
    - Creates/compares reference snapshots
    - Uses the same snapshot_update, references_dir, diff_output_dir fixtures
    - Test name format: `python_png_{variant_name}`

    Also add a cross-format highlight test to verify PNG/SVG/PDF consistency:
    ```python
    HIGHLIGHT_FORMATS = ["png", "svg", "pdf"]
    ```

    Add `test_visual_highlight_cross_format`:
    - Uses highlight_lines=["3"] on the Python fixture
    - Renders in all 3 formats
    - Creates/compares reference snapshots per format
    - Test name format: `python_{fmt}_highlight-default`

    **Integration test** -- Create `tests/test_highlights_integration.py`:

    Add integration tests that exercise the full render pipeline (not CLI, but direct API) to verify highlight behavior:

    1. `test_highlight_renders_without_error` -- Create RenderConfig with highlight_lines=["2"], render a small code snippet, assert no error.

    2. `test_highlight_word_wrap_renders_without_error` -- Create RenderConfig with highlight_lines=["1"], window_width=300, render code that will wrap, assert no error.

    3. `test_highlight_empty_list_no_change` -- Create RenderConfig with highlight_lines=[], render code, compare output bytes with config without highlights. Should be identical.

    4. `test_highlight_none_no_change` -- Same as above but highlight_lines=None.

    5. `test_highlight_all_lines` -- Highlight every line in a 5-line snippet, assert renders without error.

    Use the existing `render_fixture` helper from visual/conftest.py or replicate the pipeline inline (highlighter -> layout -> renderer).

    After writing tests, generate reference baselines:
    ```bash
    python -m pytest tests/visual/test_visual_regression.py -k highlight --snapshot-update -v
    ```

    This creates the baseline images. Then run without --snapshot-update to verify they pass.
  </action>
  <verify>
    Run: `python -m pytest tests/visual/test_visual_regression.py -k highlight -v` -- all highlight visual tests pass against baselines.
    Run: `python -m pytest tests/test_highlights_integration.py -v` -- all integration tests pass.
    Run: `python -m pytest tests/ -v --timeout=60` -- full test suite passes.
  </verify>
  <done>
    Visual regression baselines exist for: single highlight, range, mixed, custom color, word-wrap, and cross-format (PNG/SVG/PDF). Integration tests verify word-wrap highlighting, empty/None highlight lists, and all-lines highlighting. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v --timeout=60` -- full test suite passes
2. `python -m pytest tests/test_cli.py -k highlight -v` -- all CLI highlight tests pass
3. `python -m pytest tests/visual/test_visual_regression.py -k highlight -v` -- all visual highlight tests pass
4. `python -m pytest tests/test_highlights_integration.py -v` -- all integration tests pass
5. `python -m pytest tests/test_highlights.py -v` -- unit tests still pass
6. Reference images exist in tests/visual/references/ for all highlight variants
</verification>

<success_criteria>
- CLI integration tests cover: valid highlights, custom color, out-of-bounds error, invalid syntax, TOML loading
- Visual regression tests cover: single line, range, mixed, custom color, word-wrap, all 3 output formats
- Integration tests cover: basic highlight render, word-wrap + highlight, empty/None highlights (no change)
- All reference baseline images generated and tests pass against them
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-highlighting-infrastructure/12-04-SUMMARY.md`
</output>
