---
phase: 07-safety-nets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_render_renderer.py
  - tests/test_cli.py
autonomous: true

must_haves:
  truths:
    - "A test that hangs indefinitely is killed after 5 seconds (default timeout)"
    - "Slow tests get 15 seconds via @pytest.mark.timeout(15)"
    - "All 260+ existing tests pass with pytest-timeout enabled"
    - "Developers can skip slow tests locally with -m 'not slow'"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest-timeout configuration and dependency"
      contains: "timeout = 5"
    - path: "tests/test_render_renderer.py"
      provides: "Slow test markers on tests >1.2s"
      contains: "@pytest.mark.slow"
    - path: "tests/test_cli.py"
      provides: "Slow test markers on tests >1.2s"
      contains: "@pytest.mark.slow"
  key_links:
    - from: "pyproject.toml"
      to: "pytest-timeout plugin"
      via: "dev dependency + ini_options"
      pattern: "timeout = 5"
---

<objective>
Add pytest-timeout to the test suite with a 5-second default timeout and mark slow tests (>1.2s) with @pytest.mark.slow and @pytest.mark.timeout(15).

Purpose: Prevent CI hangs from individual tests — any test that exceeds its timeout is killed and reported as FAILED with a stack trace. This is the inner safety net; CI job timeout (Plan 02) is the outer net.
Output: Updated pyproject.toml with pytest-timeout dependency and config, slow markers on ~14 tests.
</objective>

<execution_context>
@/Users/bartel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bartel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-safety-nets/07-CONTEXT.md
@.planning/phases/07-safety-nets/07-RESEARCH.md
@pyproject.toml
@tests/test_render_renderer.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest-timeout and configure pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
1. Add `pytest-timeout>=2.4.0` to the `[dependency-groups] dev` list in pyproject.toml.

2. Add timeout configuration to `[tool.pytest.ini_options]`:
   - `timeout = 5` (5-second default for all tests)
   - `timeout_method = "signal"` (signal-based — project is Python-only, no threading concerns)

3. The `markers` list already declares `slow` — no change needed there.

4. Run `uv sync --dev` to install the new dependency.

5. Run `uv run pytest --co -q` to confirm pytest-timeout is loaded and test collection works.
  </action>
  <verify>
- `uv run python -c "import pytest_timeout"` succeeds (module installed)
- `uv run pytest --co -q` shows all tests collected without errors
- `grep 'timeout = 5' pyproject.toml` finds the config line
  </verify>
  <done>pyproject.toml has pytest-timeout as dev dependency with timeout=5 and timeout_method="signal" configured. Plugin is installed and pytest collects all tests.</done>
</task>

<task type="auto">
  <name>Task 2: Mark slow tests and verify full suite passes</name>
  <files>tests/test_render_renderer.py, tests/test_cli.py</files>
  <action>
1. In `tests/test_render_renderer.py`, add `import pytest` if not already imported. Add both `@pytest.mark.slow` and `@pytest.mark.timeout(15)` decorators to these test methods:
   - `TestRendererIntegration::test_render_different_themes` (2.33s)
   - `TestRendererWithShadow::test_png_with_shadow_produces_larger_output` (1.87s)
   - `TestRendererIntegration::test_full_render_with_all_features` (1.80s)
   - `TestRendererPNG::test_render_png_with_line_numbers` (1.57s)

2. In `tests/test_cli.py`, add `import pytest` if not already imported. Add both `@pytest.mark.slow` and `@pytest.mark.timeout(15)` decorators to these test methods:
   - `TestCliIntegration::test_cli_silent_on_success` (1.52s)
   - `TestCliIntegration::test_cli_generate_subprocess` (1.48s)
   - `TestCliVerbose::test_verbose_shows_steps` (1.32s)
   - `TestCliGeneration::test_stdin_with_language` (1.32s)
   - `TestCliGeneration::test_config_file` (1.32s)
   - `TestCliGeneration::test_generate_svg` (1.31s)
   - `TestCliGeneration::test_theme_flag` (1.29s)
   - `TestCliGeneration::test_generate_png` (1.29s)
   - `TestCliGeneration::test_format_flag_overrides_extension` (1.28s)
   - `TestCliGeneration::test_creates_parent_directories` (1.27s)

3. Run the full test suite: `uv run pytest`
   - ALL tests must pass
   - No tests should time out (the 5s default covers fast tests; 15s covers slow ones)

4. Run excluding slow tests to verify the marker works: `uv run pytest -m 'not slow'`
   - Should skip the 14 marked tests
   - Remaining tests should all pass

5. Run `uv run pytest --durations=5` to show the 5 slowest tests and confirm they are all marked slow.
  </action>
  <verify>
- `uv run pytest` — all tests pass, zero timeouts, zero failures
- `uv run pytest -m 'not slow'` — 14 tests deselected, rest pass
- `uv run pytest --durations=5` — top 5 slowest are all @pytest.mark.slow tests
  </verify>
  <done>All 14 tests >1.2s are marked @pytest.mark.slow and @pytest.mark.timeout(15). Full suite passes with pytest-timeout enabled. Slow tests can be skipped with -m 'not slow'.</done>
</task>

</tasks>

<verification>
1. `uv run pytest` — full suite passes with pytest-timeout active (zero timeouts, zero failures)
2. `uv run pytest -m 'not slow'` — slow tests are properly deselected
3. `grep -c 'pytest.mark.slow' tests/test_render_renderer.py tests/test_cli.py` — confirms markers present in both files
4. `grep 'timeout' pyproject.toml` — shows timeout=5 and timeout_method="signal" configured
</verification>

<success_criteria>
- pytest-timeout>=2.4.0 is a dev dependency
- Default timeout of 5 seconds applies to all tests
- 14 slow tests are marked with @pytest.mark.slow and @pytest.mark.timeout(15)
- Full test suite (260+ tests) passes without any timeouts
- `pytest -m 'not slow'` correctly deselects slow tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-safety-nets/07-01-SUMMARY.md`
</output>
