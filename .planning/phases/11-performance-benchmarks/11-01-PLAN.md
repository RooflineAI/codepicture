---
phase: 11-performance-benchmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/benchmarks/__init__.py
  - tests/benchmarks/conftest.py
  - tests/fixtures/sample_cpp.cpp
  - tests/benchmarks/fixtures/__init__.py
  - tests/benchmarks/fixtures/small.py
  - tests/benchmarks/fixtures/medium.py
  - tests/benchmarks/fixtures/large.py
autonomous: true

must_haves:
  truths:
    - "pytest-benchmark is installed and --benchmark-skip is default behavior"
    - "Running pytest without flags skips all benchmarks automatically"
    - "Shared conftest provides pre-computed inputs for each pipeline stage"
    - "C++ fixture exists alongside Python, Rust, JavaScript, MLIR"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest-benchmark dependency and --benchmark-skip in addopts"
      contains: "pytest-benchmark"
    - path: "tests/benchmarks/conftest.py"
      provides: "Shared fixtures: code strings per language, pre-tokenized lines, pre-computed metrics"
      min_lines: 60
    - path: "tests/fixtures/sample_cpp.cpp"
      provides: "C++ code fixture for benchmarking"
      min_lines: 10
    - path: "tests/benchmarks/fixtures/small.py"
      provides: "Small input (~10 lines Python)"
      min_lines: 8
    - path: "tests/benchmarks/fixtures/medium.py"
      provides: "Medium input (~50 lines Python)"
      min_lines: 40
    - path: "tests/benchmarks/fixtures/large.py"
      provides: "Large input (~200 lines Python)"
      min_lines: 150
  key_links:
    - from: "tests/benchmarks/conftest.py"
      to: "src/codepicture/highlight/pygments_highlighter.py"
      via: "PygmentsHighlighter().highlight() for pre-tokenized fixtures"
      pattern: "PygmentsHighlighter.*highlight"
    - from: "tests/benchmarks/conftest.py"
      to: "src/codepicture/layout/engine.py"
      via: "LayoutEngine().calculate_metrics() for pre-computed metrics"
      pattern: "LayoutEngine.*calculate_metrics"
---

<objective>
Install pytest-benchmark, create benchmark infrastructure (conftest with shared fixtures, directory structure), and create missing test fixtures (C++ sample, small/medium/large input files).

Purpose: All benchmark plans depend on this shared infrastructure -- fixtures that provide pre-computed inputs so each stage can be benchmarked in isolation without re-measuring upstream stages.
Output: Working benchmark directory with conftest.py, C++ fixture, and sized input fixtures.
</objective>

<execution_context>
@/Users/bartel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bartel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-performance-benchmarks/11-CONTEXT.md
@.planning/phases/11-performance-benchmarks/11-RESEARCH.md
@pyproject.toml
@src/codepicture/cli/orchestrator.py
@tests/fixtures/sample_python.py
@tests/fixtures/sample_rust.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest-benchmark and configure pytest</name>
  <files>pyproject.toml</files>
  <action>
Add pytest-benchmark to dev dependencies:
```
uv add --dev pytest-benchmark
```

Update `[tool.pytest.ini_options]` addopts to include `--benchmark-skip` so benchmarks are excluded from regular pytest runs:
```
addopts = "-v --tb=short --benchmark-skip"
```

Do NOT add a `benchmark` marker to the markers list -- pytest-benchmark uses its own `benchmark` fixture, not markers.
  </action>
  <verify>
Run `uv run pytest --co -q 2>&1 | tail -5` to confirm tests collect without benchmark errors.
Run `uv run pytest -x -q 2>&1 | tail -5` to confirm existing tests still pass with --benchmark-skip in addopts.
  </verify>
  <done>pytest-benchmark installed, --benchmark-skip in addopts, all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create benchmark directory structure, conftest, and fixtures</name>
  <files>
tests/benchmarks/__init__.py
tests/benchmarks/conftest.py
tests/fixtures/sample_cpp.cpp
tests/benchmarks/fixtures/__init__.py
tests/benchmarks/fixtures/small.py
tests/benchmarks/fixtures/medium.py
tests/benchmarks/fixtures/large.py
  </files>
  <action>
1. Create `tests/benchmarks/__init__.py` (empty).

2. Create `tests/benchmarks/fixtures/__init__.py` (empty).

3. Create `tests/fixtures/sample_cpp.cpp` -- a realistic C++ snippet (~20 lines) with includes, class definition, templates, lambdas. Something that exercises the Pygments C++ lexer well.

4. Create sized input fixtures as Python files in `tests/benchmarks/fixtures/`:
   - `small.py`: ~10 lines of realistic Python (function with docstring)
   - `medium.py`: ~50 lines of realistic Python (class with multiple methods)
   - `large.py`: ~200 lines of realistic Python (module with multiple classes, functions, decorators, type hints)
   These are stored as .py files but read as text content by conftest.

5. Create `tests/benchmarks/conftest.py` with these fixtures:
   - `register_fonts` (session-scoped autouse): calls `register_bundled_fonts()` once
   - `code_samples` (session-scoped): dict mapping language name -> code string, reading from:
     - `tests/fixtures/sample_python.py`
     - `tests/fixtures/sample_rust.rs`
     - `tests/fixtures/sample_cpp.cpp`
     - `tests/fixtures/sample_javascript.js`
     - `tests/fixtures/sample_mlir.mlir`
   - `highlighter` (session-scoped): `PygmentsHighlighter()` instance
   - `pre_tokenized` (session-scoped): dict mapping language -> tokenized output from highlighter.highlight()
   - `default_config` (session-scoped): `RenderConfig()` with defaults
   - `layout_engine` (session-scoped): `LayoutEngine(PangoTextMeasurer(), config)`
   - `pre_computed_metrics` (session-scoped): dict mapping language -> metrics from layout_engine.calculate_metrics()
   - `sized_inputs` (session-scoped): dict with keys "small", "medium", "large" mapping to code string content read from fixtures
   - Use `@pytest.mark.timeout(0)` is NOT applicable here -- instead, individual benchmark test files will disable timeout per-test.

   Import from codepicture:
   ```python
   from codepicture import (
       RenderConfig, PygmentsHighlighter, LayoutEngine,
       PangoTextMeasurer, Renderer, register_bundled_fonts, get_theme
   )
   ```

   Language mapping for highlight calls:
   - sample_python.py -> "python"
   - sample_rust.rs -> "rust"
   - sample_cpp.cpp -> "cpp"
   - sample_javascript.js -> "javascript"
   - sample_mlir.mlir -> "mlir"
  </action>
  <verify>
Run `uv run python -c "from tests.benchmarks.conftest import *; print('conftest importable')"` -- or better, run:
`uv run pytest tests/benchmarks/ --co -q 2>&1` to confirm the directory is discovered (should show 0 tests collected, no errors).
  </verify>
  <done>Benchmark directory exists with conftest.py providing all shared fixtures, C++ fixture created, small/medium/large input fixtures created</done>
</task>

</tasks>

<verification>
1. `uv run pytest -x -q` -- all existing tests pass (benchmark-skip active)
2. `uv run pytest tests/benchmarks/ --co -q` -- benchmark directory discovered, no import errors
3. `tests/fixtures/sample_cpp.cpp` exists with realistic C++ code
4. `tests/benchmarks/fixtures/small.py`, `medium.py`, `large.py` exist with appropriate sizes
</verification>

<success_criteria>
- pytest-benchmark installed and excluded from default test runs
- Benchmark conftest provides pre-computed fixtures for isolated stage benchmarking
- All 5 core language fixtures exist (Python, Rust, C++, JavaScript, MLIR)
- Sized input fixtures exist (small ~10, medium ~50, large ~200 lines)
- Existing test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/11-performance-benchmarks/11-01-SUMMARY.md`
</output>
