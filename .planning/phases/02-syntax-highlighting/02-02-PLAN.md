---
phase: 02-syntax-highlighting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codepicture/theme/__init__.py
  - src/codepicture/theme/pygments_theme.py
  - src/codepicture/theme/toml_theme.py
  - src/codepicture/theme/loader.py
  - src/codepicture/__init__.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "Theme maps Pygments token types to TextStyle (color, bold, italic)"
    - "Built-in Pygments themes are accessible by name"
    - "Catppuccin flavors (Mocha, Macchiato, Frappé, Latte) are available"
    - "Custom TOML themes can extend built-in themes"
    - "Unknown theme name raises ThemeError"
  artifacts:
    - path: "src/codepicture/theme/pygments_theme.py"
      provides: "PygmentsTheme implementing Theme protocol"
      exports: ["PygmentsTheme"]
    - path: "src/codepicture/theme/toml_theme.py"
      provides: "TOML theme parsing with inheritance"
      exports: ["load_toml_theme", "TomlTheme"]
    - path: "src/codepicture/theme/loader.py"
      provides: "Theme discovery and loading"
      exports: ["get_theme", "list_themes"]
  key_links:
    - from: "src/codepicture/theme/pygments_theme.py"
      to: "pygments.styles"
      via: "get_style_by_name, style_for_token"
      pattern: "get_style_by_name|style_for_token"
    - from: "src/codepicture/theme/loader.py"
      to: "src/codepicture/errors.py"
      via: "ThemeError for unknown themes"
      pattern: "raise ThemeError"
---

<objective>
Implement the theme system with Pygments style wrapping, TOML theme support, and theme inheritance.

Purpose: Enable syntax token colorization through a flexible theme system that supports both built-in Pygments themes and custom user themes.

Output: A working Theme implementation with get_theme() and list_themes() functions.
</objective>

<execution_context>
@/Users/bartel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bartel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-syntax-highlighting/02-CONTEXT.md
@.planning/phases/02-syntax-highlighting/02-RESEARCH.md
@src/codepicture/core/protocols.py
@src/codepicture/core/types.py
@src/codepicture/errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catppuccin dependency and create PygmentsTheme wrapper</name>
  <files>
    pyproject.toml
    src/codepicture/theme/__init__.py
    src/codepicture/theme/pygments_theme.py
  </files>
  <action>
    1. Add `catppuccin[pygments]>=2.5` to dependencies in pyproject.toml (pygments already added in 02-01)
    2. Run `uv sync` to install catppuccin

    3. Create src/codepicture/theme/ directory

    4. Create pygments_theme.py implementing PygmentsTheme class:
       - Wraps a Pygments Style class
       - Properties: name, background, foreground, line_number_fg, line_number_bg
       - get_style(token_type) -> TextStyle method
       - Use Color.from_hex() for color conversion (Pygments returns hex without #)
       - Handle None colors from style_for_token() - fall back to foreground (per RESEARCH.md Pitfall 5)

       Implementation details:
       - Constructor takes style_name: str
       - Use pygments.styles.get_style_by_name(style_name) to get style class
       - Extract background_color, highlight_color from style class attributes
       - For foreground, use style_for_token(Token) which gives default text color
       - For line numbers, derive from foreground with reduced alpha or use common convention

    5. Create __init__.py with exports (PygmentsTheme)

    Reference RESEARCH.md Pattern 2 (Token Type Inheritance) and "Extracting Style from Pygments Style Class" code example.
  </action>
  <verify>
    - `python -c "from codepicture.theme import PygmentsTheme; t = PygmentsTheme('monokai'); print(t.name, t.background.to_hex())"` prints theme info
    - `python -c "from codepicture.theme import PygmentsTheme; from pygments.token import Keyword; t = PygmentsTheme('monokai'); s = t.get_style(Keyword); print(s.color.to_hex())"` prints keyword color
  </verify>
  <done>PygmentsTheme wraps Pygments styles and maps token types to TextStyle</done>
</task>

<task type="auto">
  <name>Task 2: Implement TOML theme loading with inheritance</name>
  <files>
    src/codepicture/theme/toml_theme.py
    src/codepicture/theme/loader.py
    src/codepicture/theme/__init__.py
    src/codepicture/__init__.py
  </files>
  <action>
    1. Create toml_theme.py:
       - Define TomlTheme class implementing Theme protocol
       - Constructor takes parsed TOML dict and optional base theme
       - Parse [colors] section for background, foreground
       - Parse [tokens] section for token-specific styles
       - Token names map to Pygments token types (e.g., "Keyword" -> Token.Keyword)
       - Support inheritance: unspecified tokens fall back to base theme
       - Support token type inheritance within theme (String.Escape -> String -> Text)

       Helper function:
       - parse_token_name(name: str) -> Token: Convert "Keyword.Constant" to Token.Keyword.Constant

    2. Create loader.py with:
       - BUILTIN_THEMES: dict mapping names to constructors
         Include: dracula, monokai, one-dark, catppuccin-mocha, catppuccin-macchiato, catppuccin-frappe, catppuccin-latte
         (Catppuccin styles registered by catppuccin[pygments] package)
       - get_theme(name: str, custom_path: Path | None = None) -> Theme:
         - If name in BUILTIN_THEMES, return PygmentsTheme(name)
         - If custom_path provided, load TOML and return TomlTheme
         - Else raise ThemeError with available themes
       - list_themes() -> list[str]: Return sorted list of available theme names

    3. Update theme/__init__.py to export: PygmentsTheme, TomlTheme, get_theme, list_themes

    4. Update src/codepicture/__init__.py to export theme functions

    Reference RESEARCH.md Pattern 3 (Theme TOML Format with Inheritance).

    CONTEXT.md decisions:
    - Default theme: catppuccin-mocha (when no config)
    - Theme inheritance via `extends = "theme-name"`
    - Invalid/missing theme: error and exit (ThemeError)
  </action>
  <verify>
    - `python -c "from codepicture.theme import get_theme; t = get_theme('catppuccin-mocha'); print(t.name)"` prints "catppuccin-mocha"
    - `python -c "from codepicture.theme import list_themes; print(list_themes())"` shows all available themes
    - `python -c "from codepicture.theme import get_theme; get_theme('notarealtheme')"` raises ThemeError
  </verify>
  <done>
    Theme loader provides get_theme() for built-in and TOML themes, list_themes() for discovery, and ThemeError for unknowns
  </done>
</task>

</tasks>

<verification>
1. Import check: `python -c "from codepicture import get_theme, list_themes"`
2. All Catppuccin flavors work:
   ```python
   from codepicture.theme import get_theme
   for flavor in ['catppuccin-mocha', 'catppuccin-macchiato', 'catppuccin-frappe', 'catppuccin-latte']:
       t = get_theme(flavor)
       assert t.name == flavor
   ```
3. Token styling works:
   ```python
   from codepicture.theme import get_theme
   from pygments.token import Keyword, String, Comment
   t = get_theme('monokai')
   for token in [Keyword, String, Comment]:
       style = t.get_style(token)
       assert style.color is not None
   ```
4. Theme list includes expected themes: dracula, monokai, one-dark, catppuccin-mocha
</verification>

<success_criteria>
- PygmentsTheme satisfies the Theme protocol from protocols.py
- All 4 Catppuccin flavors are available via get_theme()
- Built-in Pygments themes (dracula, monokai, one-dark) are accessible
- ThemeError raised with available themes for unknown theme name
- Token type inheritance works (String.Escape falls back to String)
</success_criteria>

<output>
After completion, create `.planning/phases/02-syntax-highlighting/02-02-SUMMARY.md`
</output>
