---
phase: 04-rendering
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/codepicture/render/chrome.py
  - src/codepicture/render/__init__.py
autonomous: true

must_haves:
  truths:
    - "Traffic light buttons render as red/yellow/green circles"
    - "Title bar height is 28px (macOS standard)"
    - "Buttons are left-aligned with correct spacing"
    - "Title text (when provided) is centered in title bar"
    - "No chrome mode skips title bar entirely"
  artifacts:
    - path: "src/codepicture/render/chrome.py"
      provides: "Window chrome rendering functions"
      exports: ["draw_title_bar", "draw_traffic_lights", "TITLE_BAR_HEIGHT"]
    - path: "src/codepicture/render/__init__.py"
      provides: "Updated exports including chrome"
      exports: ["CairoCanvas", "draw_title_bar", "draw_traffic_lights", "TITLE_BAR_HEIGHT"]
  key_links:
    - from: "src/codepicture/render/chrome.py"
      to: "src/codepicture/render/canvas.py"
      via: "Uses CairoCanvas for drawing"
      pattern: "def draw_title_bar.*canvas.*CairoCanvas"
    - from: "src/codepicture/render/chrome.py"
      to: "src/codepicture/core/types.py"
      via: "Uses Color for button and background colors"
      pattern: "from.*types import.*Color"
---

<objective>
Implement macOS-style window chrome with title bar and traffic light buttons.

Purpose: Window chrome gives the rendered code a polished macOS window appearance, which is a key visual feature of the tool. Per CONTEXT.md, the buttons are static (no hover states) and left-aligned.

Output: Chrome module with draw_title_bar and draw_traffic_lights functions, plus constants for consistent sizing.
</objective>

<execution_context>
@/Users/bartel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bartel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rendering/04-RESEARCH.md
@.planning/phases/04-rendering/04-CONTEXT.md
@.planning/phases/04-rendering/04-01-SUMMARY.md
@src/codepicture/render/canvas.py
@src/codepicture/core/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chrome module with traffic lights and title bar</name>
  <files>
    src/codepicture/render/chrome.py
  </files>
  <action>
    Create chrome.py with window chrome rendering:

    1. Define constants (based on RESEARCH.md macOS values):
       ```python
       # Title bar
       TITLE_BAR_HEIGHT = 28  # px, macOS standard

       # Traffic light buttons
       BUTTON_DIAMETER = 12    # px
       BUTTON_SPACING = 8      # px between buttons
       BUTTON_MARGIN_LEFT = 8  # px from left edge

       # Button colors (static, per CONTEXT.md)
       CLOSE_COLOR = Color.from_hex("#ff5f57")      # Red
       MINIMIZE_COLOR = Color.from_hex("#febc2e")   # Yellow
       MAXIMIZE_COLOR = Color.from_hex("#28c840")   # Green
       ```

    2. Implement draw_traffic_lights(canvas, title_bar_y):
       - Calculate button_y to center vertically in title bar (title_bar_y + (TITLE_BAR_HEIGHT - BUTTON_DIAMETER) / 2)
       - Loop through colors [CLOSE, MINIMIZE, MAXIMIZE]
       - For each: calculate center_x, center_y
       - Call canvas.draw_circle(center_x, center_y, BUTTON_DIAMETER/2, color)

    3. Implement draw_title_bar(canvas, width, background, title=None, corner_radius=0):
       - Draw title bar background rectangle (full width, TITLE_BAR_HEIGHT)
       - If corner_radius > 0, use rounded top corners only:
         * Draw full rounded rect at y=0
         * This gives rounded top corners for window appearance
       - Call draw_traffic_lights(canvas, 0)
       - If title is provided:
         * Use system font fallback ("SF Pro", "Helvetica Neue", "Helvetica", "Arial")
         * Center title text horizontally in title bar
         * Draw text with foreground color (slightly muted for title)
         * Font size: 13px (macOS title font size)

    4. Type hints:
       - canvas: Use TYPE_CHECKING import for CairoCanvas type hint
       - Avoid circular imports by using string annotations or TYPE_CHECKING guard
  </action>
  <verify>
    python -c "from codepicture.render.chrome import draw_title_bar, draw_traffic_lights, TITLE_BAR_HEIGHT; print(f'Title bar height: {TITLE_BAR_HEIGHT}px')"
  </verify>
  <done>
    Traffic light buttons draw at correct positions with macOS colors. Title bar draws with optional title text centered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update render module exports</name>
  <files>
    src/codepicture/render/__init__.py
  </files>
  <action>
    Update __init__.py to export chrome module contents:
    - Import draw_title_bar, draw_traffic_lights, TITLE_BAR_HEIGHT from chrome
    - Add to __all__ list
  </action>
  <verify>
    python -c "from codepicture.render import draw_title_bar, draw_traffic_lights, TITLE_BAR_HEIGHT; print('Chrome exports OK')"
  </verify>
  <done>
    Chrome functions are exported from render module for use by Renderer.
  </done>
</task>

</tasks>

<verification>
- All existing tests pass: `pytest tests/`
- Traffic light button constants match RESEARCH.md values
- draw_traffic_lights creates three circles at expected positions
- draw_title_bar handles both with and without title text
- No circular import errors when importing render module
</verification>

<success_criteria>
1. Traffic light buttons have correct colors (#ff5f57, #febc2e, #28c840)
2. Buttons are 12px diameter with 8px spacing
3. Title bar is 28px height
4. Title text (when provided) is centered
5. Functions are exported from render module
</success_criteria>

<output>
After completion, create `.planning/phases/04-rendering/04-02-SUMMARY.md`
</output>
