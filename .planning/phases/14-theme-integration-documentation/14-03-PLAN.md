---
phase: 14-theme-integration-documentation
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - README.md
  - docs/generate-examples.sh
  - docs/examples/highlight-dark.png
  - docs/examples/highlight-light.png
  - docs/examples/highlight-focus.png
autonomous: false
requirements: [HLDOC-01, HLDOC-02, HLDOC-03]

must_haves:
  truths:
    - "README has a '## Line Highlighting' section with Quick Start, Highlight Styles, Configuration, and Theme Integration subsections"
    - "README documents --highlight CLI flag with usage examples"
    - "README includes copy-paste-ready TOML config examples with inline comments"
    - "README embeds ~3 example images from docs/examples/"
    - "Example images are auto-generated by docs/generate-examples.sh"
  artifacts:
    - path: "README.md"
      provides: "Line Highlighting documentation section"
      contains: "## Line Highlighting"
    - path: "docs/generate-examples.sh"
      provides: "Script to auto-generate example images"
    - path: "docs/examples/highlight-dark.png"
      provides: "Dark theme example image"
    - path: "docs/examples/highlight-light.png"
      provides: "Light theme example image"
    - path: "docs/examples/highlight-focus.png"
      provides: "Focus mode example image"
  key_links:
    - from: "README.md"
      to: "docs/examples/"
      via: "Markdown image references"
      pattern: "docs/examples/highlight-"
    - from: "docs/generate-examples.sh"
      to: "codepicture CLI"
      via: "codepicture commands"
      pattern: "codepicture .* --highlight"
---

<objective>
Add complete Line Highlighting documentation to README with auto-generated visual examples showing highlight styles on dark and light themes.

Purpose: Users need to discover and learn the highlighting feature through clear docs with visual examples. Per D-07/D-08/D-09/D-10.

Output: README with new Line Highlighting section, example generation script, and 3 committed example images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-theme-integration-documentation/14-CONTEXT.md
@.planning/phases/14-theme-integration-documentation/14-RESEARCH.md
@.planning/phases/14-theme-integration-documentation/14-01-SUMMARY.md

@README.md
@src/codepicture/render/highlights.py
@src/codepicture/config/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create example generation script and generate images</name>
  <files>docs/generate-examples.sh, docs/demo.py, docs/examples/highlight-dark.png, docs/examples/highlight-light.png, docs/examples/highlight-focus.png</files>
  <read_first>
    README.md
    src/codepicture/render/highlights.py
  </read_first>
  <action>
    1. Create docs/ directory if it doesn't exist.
    2. Create docs/examples/ directory.
    3. Create a small demo code snippet at docs/demo.py that is 12-15 lines of simple, readable Python (e.g., a function that greets users with a class). The content should be recognizable and look good in screenshots. Example:

    ```python
    class Greeter:
        """A friendly greeter."""

        def __init__(self, name: str):
            self.name = name

        def greet(self) -> str:
            return f"Hello, {self.name}!"

        def farewell(self) -> str:
            return f"Goodbye, {self.name}!"

    greeter = Greeter("World")
    print(greeter.greet())
    ```

    4. Create docs/generate-examples.sh (per D-09):

    ```bash
    #!/usr/bin/env bash
    # Generate example images for README documentation.
    # Run from project root: bash docs/generate-examples.sh
    set -euo pipefail

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    EXAMPLES_DIR="$SCRIPT_DIR/examples"
    DEMO="$SCRIPT_DIR/demo.py"

    mkdir -p "$EXAMPLES_DIR"

    echo "Generating dark theme example (catppuccin-mocha)..."
    codepicture "$DEMO" -o "$EXAMPLES_DIR/highlight-dark.png" \
      --theme catppuccin-mocha \
      --highlight '1-3:highlight' \
      --highlight '5-6:add' \
      --highlight '8-9:remove' \
      --highlight '11-12:focus' \
      --no-shadow

    echo "Generating light theme example (catppuccin-latte)..."
    codepicture "$DEMO" -o "$EXAMPLES_DIR/highlight-light.png" \
      --theme catppuccin-latte \
      --highlight '1-3:highlight' \
      --highlight '5-6:add' \
      --highlight '8-9:remove' \
      --highlight '11-12:focus' \
      --no-shadow

    echo "Generating focus mode example..."
    codepicture "$DEMO" -o "$EXAMPLES_DIR/highlight-focus.png" \
      --theme catppuccin-mocha \
      --highlight '5-9:focus' \
      --no-shadow

    echo "Done. Images saved to $EXAMPLES_DIR/"
    ls -la "$EXAMPLES_DIR/"
    ```

    5. Make the script executable: `chmod +x docs/generate-examples.sh`
    6. Run the script: `bash docs/generate-examples.sh`
    7. Verify all 3 images exist and are > 1KB.

    NOTE: Adjust line numbers in the --highlight flags if the demo snippet has different line counts. The goal is to show all 4 styles in the dark/light examples and a focus-only example.
  </action>
  <verify>
    <automated>cd /Users/bartel/Documents/newclone/codepicture && bash docs/generate-examples.sh && test -f docs/examples/highlight-dark.png && test -f docs/examples/highlight-light.png && test -f docs/examples/highlight-focus.png && echo "ALL IMAGES GENERATED"</automated>
  </verify>
  <acceptance_criteria>
    - docs/generate-examples.sh exists and is executable
    - docs/generate-examples.sh contains `codepicture` commands with `--highlight` flags
    - docs/demo.py exists with 10+ lines of Python code
    - docs/examples/highlight-dark.png exists and is > 1KB
    - docs/examples/highlight-light.png exists and is > 1KB
    - docs/examples/highlight-focus.png exists and is > 1KB
    - `bash docs/generate-examples.sh` exits 0
  </acceptance_criteria>
  <done>Example generation script runs successfully and produces 3 PNG images showing highlight styles on dark theme, light theme, and focus mode.</done>
</task>

<task type="auto">
  <name>Task 2: Add Line Highlighting section to README</name>
  <files>README.md</files>
  <read_first>
    README.md
    src/codepicture/render/highlights.py
    src/codepicture/config/schema.py
    docs/generate-examples.sh
  </read_first>
  <action>
    Add a new `## Line Highlighting` section to README.md AFTER the `## Configuration` section (which ends around line 195 with "CLI flags always override config file values.") and BEFORE the `## System Dependencies` section (per D-07).

    The section must contain these subsections (per D-07):

    ### Quick Start

    Show basic usage:
    ```bash
    # Highlight lines 3-5 with default style
    codepicture code.py -o output.png --highlight '3-5'

    # Highlight with named styles
    codepicture code.py -o output.png \
      --highlight '3-5:add' \
      --highlight '10:remove'
    ```

    ### Highlight Styles

    Table of the 4 built-in styles with descriptions:

    | Style | Usage | Effect |
    |-------|-------|--------|
    | `highlight` | `--highlight '3-5'` or `--highlight '3-5:highlight'` | Yellow background (default style) |
    | `add` | `--highlight '3-5:add'` | Green background with `+` gutter indicator |
    | `remove` | `--highlight '3-5:remove'` | Red background with `-` gutter indicator |
    | `focus` | `--highlight '3-5:focus'` | Blue background; all other lines are dimmed |

    Repeat `--highlight` for multiple groups:
    ```bash
    codepicture diff.py -o diff.png \
      --highlight '1-3:add' \
      --highlight '7-9:remove' \
      --highlight '12:highlight'
    ```

    Embed the dark and light theme example images (per D-10). Write these as actual markdown image references (NOT inside a code block):

    **Dark theme (Catppuccin Mocha):**

    ![Highlight styles on dark theme](docs/examples/highlight-dark.png)

    **Light theme (Catppuccin Latte):**

    ![Highlight styles on light theme](docs/examples/highlight-light.png)

    **Focus mode** dims unfocused lines for emphasis:

    ![Focus mode example](docs/examples/highlight-focus.png)

    ### Configuration (TOML)

    Copy-paste-ready TOML examples with inline comments (per D-08). Document both the highlights entries and per-style color overrides:

    ```toml
    # Highlight specific lines (equivalent to --highlight CLI flags)
    highlights = ["3-5:add", "10:remove", "15-20:focus"]

    # Customize highlight style colors (optional)
    # Colors use #RRGGBB (25% opacity applied) or #RRGGBBAA format
    [highlight_styles.add]
    color = "#00CC4040"       # green at 25% opacity

    [highlight_styles.remove]
    color = "#FF333340"       # red at 25% opacity

    [highlight_styles.focus]
    color = "#3399FF40"       # blue at 25% opacity

    [highlight_styles.highlight]
    color = "#FFE65040"       # yellow at 25% opacity
    ```

    IMPORTANT: Do NOT document `--highlight-color` as a CLI flag. It was removed in Phase 13. The `highlight_color` field is TOML-only (per RESEARCH.md pitfall 2).

    ### Theme Integration

    Explain that highlight colors automatically adapt to light vs dark themes:

    codepicture detects whether the active theme has a light or dark background
    and selects an appropriate highlight color palette. Dark themes use bright,
    vivid overlay colors; light themes use darker, muted overlays for better
    contrast. User-specified colors in TOML config always override the
    theme-derived defaults.
  </action>
  <verify>
    <automated>cd /Users/bartel/Documents/newclone/codepicture && grep -c "## Line Highlighting" README.md && grep -c "### Quick Start" README.md && grep -c "### Highlight Styles" README.md && grep -c "### Configuration" README.md && grep -c "### Theme Integration" README.md && grep -c "docs/examples/highlight-" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Line Highlighting`
    - README.md contains `### Quick Start` (within Line Highlighting section)
    - README.md contains `### Highlight Styles`
    - README.md contains `### Configuration` (TOML subsection)
    - README.md contains `### Theme Integration`
    - README.md contains `--highlight '3-5:add'` (CLI flag documented, per HLDOC-01)
    - README.md contains `[highlight_styles.add]` (TOML config documented, per HLDOC-02)
    - README.md contains `docs/examples/highlight-dark.png` (visual example, per HLDOC-03)
    - README.md contains `docs/examples/highlight-light.png`
    - README.md contains `docs/examples/highlight-focus.png`
    - README.md does NOT contain `--highlight-color` (removed in Phase 13)
  </acceptance_criteria>
  <done>README has complete Line Highlighting section with CLI usage examples, style table, TOML config examples, theme integration explanation, and 3 embedded example images.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify README documentation and example images</name>
  <files>README.md, docs/examples/highlight-dark.png, docs/examples/highlight-light.png, docs/examples/highlight-focus.png</files>
  <action>
    Present the completed documentation and images for human review. The executor should display paths to the example images and summarize the README changes.
  </action>
  <what-built>
    Complete Line Highlighting documentation in README with auto-generated example images showing all 4 highlight styles on dark and light themes, plus focus mode.
  </what-built>
  <how-to-verify>
    1. Open README.md and read the new "## Line Highlighting" section
    2. Verify the section has: Quick Start, Highlight Styles, Configuration (TOML), Theme Integration subsections
    3. Open docs/examples/highlight-dark.png -- should show all 4 highlight styles on catppuccin-mocha
    4. Open docs/examples/highlight-light.png -- should show all 4 highlight styles on catppuccin-latte with muted colors
    5. Open docs/examples/highlight-focus.png -- should show focus mode with dimmed unfocused lines
    6. Verify the TOML examples are copy-paste-ready
    7. Verify --highlight-color is NOT documented as a CLI flag
  </how-to-verify>
  <verify>Human confirms documentation and images are correct</verify>
  <done>Human has approved the README documentation and example images</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `bash docs/generate-examples.sh` succeeds and produces 3 images
- README.md contains all required sections and image references
- No references to removed --highlight-color CLI flag
</verification>

<success_criteria>
- README documents all highlight CLI flags with examples (HLDOC-01)
- README documents TOML config with copy-paste examples (HLDOC-02)
- README includes 3 visual examples (HLDOC-03)
- Example images are auto-generated and committed
- Human approves documentation quality
</success_criteria>

<output>
After completion, create `.planning/phases/14-theme-integration-documentation/14-03-SUMMARY.md`
</output>
